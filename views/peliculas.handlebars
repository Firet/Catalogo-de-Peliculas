<h1>Películas</h1>
<p>Listado de películas</p>

{{#each pelicula}}
  <h2>
    <a href="/pelicula/{{@index}}">{{this.section}}</a>
  </h2>
  <ul>
    {{#each items}}
      <li>{{this}}</li>
    {{/each}}
  </ul>
{{/each}}